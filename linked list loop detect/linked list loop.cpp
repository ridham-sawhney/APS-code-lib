#include<bits/stdc++.h>
using namespace std;

// class node{
//     public:
//     int data;
//     node* next;

//     node(int d)
//     {
//         data=d;
//         next=NULL;
//     }
// };

// node* insert(node* head,int data)
// {
// node * newnode=new node(data);
// if(head==NULL)
// head=newnode;
// else
// newnode->next=head;

// return newnode;
// }

// void display(node * root)
// {
//     while(root!=NULL)
//     {
//         cout<<root->data<<" ";
//         root=root->next;
//     }
// }

int main()
{
    cout<<"je";
    // node* root=NULL;
    // root=insert(root,1);
    // root=insert(root,2);
    // root=insert(root,3);
    // root=insert(root,4);
    // display(root);
    return 0;
}